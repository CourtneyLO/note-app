(function(exports) {
  var Note = function(text, id) {
    this.text = text;
    this._id = id;
  }

  Note.prototype.addNotes = function() {
      return text;
  };

  exports.Note = Note;
})(this);
